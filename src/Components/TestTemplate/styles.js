import styled from 'styled-components';

export const TestTemplate = styled.section`
  justify-self: center;
  width: 100%;
  text-align: center;
  padding: 0.5rem;
  background: var(--black);
  span {
    color: var(--white);
    font-size: 1.2rem;
  }
`;
